spring:
  datasource:
    url: jdbc:h2:file:./data/continuum;AUTO_SERVER=TRUE
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

server:
  port: 8090
